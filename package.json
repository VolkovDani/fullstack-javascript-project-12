{
  "name": "@hexlet/code",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build-project": "npm run build-project --prefix frontend",
    "start": "start-server -s ./frontend/build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "cd frontend && npm ci"
  },
  "author": "Danil Volkov (volchara287@gmail.com)",
  "license": "ISC",
  "dependencies": {
    "@hexlet/chat-server": "^2.0.3"
  }
}
